#include <stdio.h>

/**
 * puts2 - prints every other character of a string
 * @str: the string to print
 *
 * Return: void
 */
void puts2(char *str)
{
  int i,x;
  while (str[x] != '\0')
    x++;

for (i = 0; i < j; i += 2)
putchar(str[i]);

putchar('\n');

}
